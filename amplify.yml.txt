version: 1
applications:
  - appRoot: EventSphere/Frontend   # 👈 use the exact folder path to package.json
    frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build
    artifacts:
      baseDirectory: dist   # 👈 Vite default build output
      files:
        - '**/*'
    cache:
      paths:
        - node_modules/**/*
