version: 1
applications:
  - appRoot: EventSphere/Frontend   # 👈 path to your package.json
    frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: dist          # Vite default output
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
