version: 1
applications:
  - appRoot: EventSphere/Frontend   # 👈 exact path to your package.json
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci   # faster & safer than npm install on Amplify
        build:
          commands:
            - npm run build
    artifacts:
      baseDirectory: dist   # 👈 Vite build output folder
      files:
        - '**/*'
    cache:
      paths:
        - node_modules/**/*
